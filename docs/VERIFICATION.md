# 软件验证 · 2026-09-07

本地 Windows，Python 3.8.8；隔离 venv 使用已有系统依赖，两个模块均通过普通安装构建 wheel 后导入，未使用 editable 安装。

| 检查 | 本次结果 |
|---|---|
| 原活动量 unittest | 17/17 通过 |
| 安装后输出符合各自 JSON Schema | 通过，两个模块均检查 |
| 接包后状态恢复与未来快照一致 | 通过，两个模块均检查 |
| 错误绑定拒收且不修改状态 | 通过，两个模块均检查 |
| 两模块合成 V2 包演示 | 通过，活动证据不足时输出不可用，未生成融合报警 |
| 运行源码、setup.py 与原交付 SHA-256 | 13 个文件一致 |
| 仓库 Markdown 相对文件链接 | 检查通过 |

新增接口测试共 3 项，分别覆盖两个模块。源码整理未修改模型参数和已有模块实现。
GitHub CI 另配置 Python 3.10/3.12，远端运行结果以 Actions 为准，不将配置等同已通过。

本次未重跑真实产犊实验，也没有开展独立牛验证、模型重训或融合决策评估。
原活动实测报告及 `modules/activity/验证/结果` 是随交付导入的历史记录；温度旧验证目录和活动原始包目录缺失情况见 MIGRATION.md。

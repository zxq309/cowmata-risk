# 2026-09-07 导入记录

来源为本地 `COWMATA_20260906/综合决策20260906`。原交付目录完整保留。
温度模块映射到 `modules/temperature`，活动模块映射到 `modules/activity`；包名、模块版本、算法及参数保留。
逐文件初始 SHA-256 见 [import-manifest.json](import-manifest.json)。模块 README、报告相对路径按新结构整理。

活动量的 `验证/结果` 中 CSV/JSON 为原交付历史材料，未在本次整理中重跑真实实验。
大型 PDF、逐包完整 JSONL、状态快照和临时安装环境保留原目录；仓库保留 PNG 图和摘要/审计表。
温度 README 所指 `算法验证/20260905_纯净交付_v6` 未出现在本次提供目录中，不能声称已导入或复验。
温度数据库与原标注保留原位置，不纳入本次源码仓。

原活动实测是开发数据上的可复现性验收，不是独立预测性能。本次软件测试记录另见 VERIFICATION.md。
新提交只整理工程边界；不存在融合模型已训练或部署的含义。
